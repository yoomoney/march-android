task increaseVersionCode {
    group 'versions'
    doLast {
        File versionFile = file(".versionCode")
        Integer versionCode = ++getAppVersionCode()
        versionFile.write(versionCode.toString())
    }
}

